# Security policy

Security reports should describe the affected file, workflow, package behavior, or upstream artifact and include reproducible evidence where possible.

Open a private GitHub security advisory for vulnerabilities that should not be disclosed publicly before a fix is available. Use a normal issue for non-sensitive packaging bugs.

Automated release updates are accepted only after upstream signature verification, Nix hash generation, full flake checks, and a successful package build.
